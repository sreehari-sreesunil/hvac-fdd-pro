/**
 * MOCK DATA — illustrative eval numbers for the marketing page only. The
 * copilot chat itself is wired to the real copilot-service backend
 * (see frontend/lib/api/copilot.ts); this file no longer backs any chat UI.
 */

export const MOCK_EVAL_METRICS = {
  precision: 0.91,
  recall: 0.87,
  f1: 0.89,
  evaluatedFaultClasses: 12,
};
